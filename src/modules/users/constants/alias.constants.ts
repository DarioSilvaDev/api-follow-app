/** D-091: cooldown de cambio de alias en días. */
export const ALIAS_COOLDOWN_DAYS = 15;

/** D-077: regex de alias público (formato de la spec y DECISION-REGISTER). */
export const ALIAS_PATTERN = /^[a-z0-9._-]{3,30}$/;
